---
description: Intentionally bad rules fixture for R10 authoring-form evals
---

# Packaged Rule: Sample Domain Discovery (broken fixture)

## Requirements

- Prefer visible text selectors because they are easier for humans to read.
- First write the domain file, then add credentials so that agents can log in.
- Store passwords in the domain file which allows reuse across flows.
